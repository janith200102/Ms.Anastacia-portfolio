## Packages
framer-motion | Complex animations and page transitions
react-icons | Icon library for the services and contact sections
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
- "Why Choose Me" is implemented as a dedicated page per the 6-page requirement, but linked prominently.
- Images are imported statically from @assets as requested.
- The contact form uses the shared schema and API route /api/contact.
- A dark luxury theme is applied using Tailwind colors and custom CSS variables.
